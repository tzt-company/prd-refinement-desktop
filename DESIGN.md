---
version: alpha
name: "需求显微镜"
description: "面向产品、研发和测试的 PRD 需求细化桌面工作台，以来源覆盖轨道呈现需求证据链。"
colors:
  ink: "#172027"
  navy: "#17324A"
  primary: "#246DA8"
  evidence: "#5EB8BD"
  warning: "#D69635"
  paper: "#F7F9FA"
  border: "#CCD5DA"
typography:
  sans:
    fontFamily: "Microsoft YaHei UI, Noto Sans SC, system-ui, sans-serif"
  mono:
    fontFamily: "Cascadia Mono, ui-monospace, monospace"
rounded:
  DEFAULT: "0.5rem"
  sm: "0.25rem"
  md: "0.5rem"
  lg: "0.75rem"
spacing:
  panel: "1rem"
  section: "1.5rem"
components:
  projectRail: {}
  sourceTrack: {}
  requirementList: {}
  clarificationDrawer: {}
---

# 需求显微镜 Design System

## Overview

创意参照是“云平台任务控制台”：上传页只引导一个主动作，执行页显示可理解的阶段进度，结果页先给汇总再按标签进入功能与需求明细。用户是需要把大 PRD 交给 Agent 的产品、研发和测试人员，主要场景是 Windows 桌面端的长时间审阅。

界面使用中文。产品层级和状态应安静、清楚；唯一强调元素是青色来源轨道。避免聊天窗口、通用 AI 渐变、营销式大数字和大面积卡片堆叠。本文是设计令牌与意图的权威来源，运行时映射位于 `src/styles.css` 的 `:root`。

运行时设置默认同时执行 5 个任务，每个任务最多并发 10 个分析节点；设置页展示的取值范围必须与调度器实际限制一致。

## Colors

深海军蓝用于项目导航，蓝色表示当前操作，青色表示来源、证据和完整处理。阻塞、建议、可忽略分别使用红、琥珀和中性灰，并始终配合文字标签。背景使用冷白和浅灰，保证长时间阅读中的层级稳定。

## Typography

中文正文使用 Microsoft YaHei UI 与 Noto Sans SC 回退；需求编号使用 Cascadia Mono。内部来源编号不出现在业务阅读界面。标题保持紧凑，正文行高增加，以支持长条件和验收断言。

## Layout

桌面应用以任务中心为平台入口，每个任务内部由上传、执行过程、结果汇总三阶段组成。任务中心用资源表呈现并行状态；执行页按阶段显示耗时；结果页使用概览、功能与需求、全部需求、待处理事项四个标签。“功能与需求”展示模型生成的简短功能名称、原文位置和需求数量，不展示内部类型列；名称仅用于导航。待处理事项在同一入口按阻塞、建议、可忽略和平台问题筛选，详情抽屉解释事实、未决点、影响、分级理由、处理方和原文证据。统一 Runtime 配置使用独立设置页。最低宽度为 1100px。

任务列表以“任务编号 + 项目名称”为第一扫描层，来源文件降为第二层；状态、阶段、进度和时间使用稳定列宽。任务行不得低于 84px，避免三层任务信息与运行指标互相挤压；整行作为进入详情的唯一操作，并提供 hover 与键盘焦点反馈。

任务状态只有在当前快照通过平台检查且不存在阻塞级业务澄清时显示“可交付”。平台问题或阻塞澄清仍存在时显示“平台整理未完成”，保留草稿、逐项诊断和重新审计入口。“阻塞 / 建议 / 可忽略”只用于业务澄清级别；平台责任问题使用执行状态表达，不混入业务等级。

功能页在列表上方提供交付范围选择。选择控件只表达用户要交给开发 Agent 的功能范围；系统仍会把范围内的阻塞或未验证内容隔离到待处理清单。范围选择、功能导航和“调整细化”是三个独立操作，不能依靠点击整行猜测意图。

澄清抽屉要求用户明确选择填写答案、补充说明或不适用。提交后进入新的结果版本；生成中、失败和冲突都保留本次输入，用户可以重试或回到最新版本重新提交。

平台问题先基于完整相关原文和同功能需求确认成立，再进入对应责任节点。局部复核分别展示原目标、候选回归和旁支发现；原目标完成且无回归时保留修改，旁支发现另行处理，避免用户看到平台对同一内容重复整理。

## Elevation & Depth

依靠色块、分隔线和固定栏位表达层级。静态卡片不使用阴影；焦点与选中状态用描边和内侧标记表达。

## Shapes

操作控件与信息容器使用 8–12px 圆角；来源轨道节点保持圆形。避免胶囊形状成为通用装饰。

## Components

按钮、搜索框和可选行均提供 `focus-visible`。加载保持原布局；失败信息必须说明失败对象和可恢复动作。图标来自同一线性图标族，主要操作保留文字标签。动效只用于阶段状态变化，并尊重 reduced motion。

## Do's and Don'ts

- Do：让每项明细始终能够看见来源、状态、条件和 PRD 原文明示的验收条件；不生成测试场景。
- Do：使用固定编号和稳定列宽帮助高密度扫描。
- Don't：将 Agent 对话作为主界面或让聊天记录替代结构化产物。
- Don't：用“覆盖率 100%”掩盖未读材料或未解决的语义问题。


业务结果默认显示四个阅读入口，来源处置只保留为 Excel 隐藏追溯表。自动检查记录用于解释平台发现和处理过什么；待处理事项用于说明现在由谁处理什么，两者不重复要求用户判断。需求与事项详情抽屉为非模态补充阅读区域，支持 Escape 关闭。桌面本地阅读的筛选暂存于页面组件，不写入 URL。

正文与表格使用 12–13px 基线，避免原有 8–9px 小字号；明细行保留足够行高，来源抽屉以 13px 展示文本。

## 资料包准备

新建任务由 `src/MaterialWorkspace.tsx` 统一承接。入口沿用原 PRD 上传页的居中单卡片：首要动作始终是“选择 PRD 文件”，不要求用户先理解资料包、版本或索引。选择主 PRD 后，再渐进展开补充文件、目录和识别；高密度清单仅在已有材料时出现。索引进度只显示实际完成文件数，按钮和固定反馈区明确区分保存、索引与创建任务。

已有资料包的“重命名”和“删除资料包”直接显示在恢复选择器旁，不再经过通用管理入口。重命名显式保存；整体删除使用页面内二次确认，并在确认文案中说明删除范围和原始文件不受影响。资料处理中禁用这两个动作。

选中主 PRD 后切换为左对齐的资料准备工作态，内容宽度统一为 1040px。视觉顺序固定为主 PRD 身份、可选补充资料、当前下一步和资料明细。当前可执行步骤使用唯一主按钮；尚不可执行的后续分析保持次要层级。系统只读取用户上传的文件，不显示或判断缺件。
