# HTML 导入阻断修复

真实任务 T-36AB17B1 在原文建账失败。原规则把所有 style、脚本和 iframe 当作未读取附件，无法区分排版与内容。原文件 SHA-256 必须与任务一致，修复前备份任务。任务尚未进入模型环节，允许以同一原文件重建导入单元，不保留失真的旧解析结果。

方案：普通排版属性不阻断；样式与脚本源代码保留为来源说明，不执行；递归解析 srcdoc 与 data:text/html 内嵌文档，限制嵌套深度；真实外部 iframe、缺失图片和无法解析资源继续阻断，并报告对象。原型引用的 assets/filter.svg 和 assets/copy-document.svg 原目录不存在，不能伪造或当作已读。

验证：样式非阻断、内嵌正文与脚本字面量保留、外部 iframe/图片仍阻断、嵌套限制、真实原文件重建和任务恢复。模型分析和交付需要真实来源完整后再运行。
