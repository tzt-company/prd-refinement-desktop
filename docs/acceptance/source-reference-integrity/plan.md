# 原文引用完整性验收计划

需求、根因、方案和边界以 `docs/spec/source-reference-integrity.md` 为准。本目录只记录实施验证。

按以下顺序实施：来源结构与证据目录；模型协议和领域引用解析；统一/拆分/返工区间保持；页面与导出；真实 Electron 新任务及业务规则反查。每轮结果写入新的 `round-N.md`，总状态以 `matrix.csv` 为准；全部通过后才生成 `report.md`。
