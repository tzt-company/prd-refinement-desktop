# Round 8：pipeline 11 执行预算与来源处置验证

## 已通过

- `npm test`：16 个测试文件、175 项测试全部通过。
- `npm run build`：TypeScript、Vite 生产构建及 Electron TypeScript 编译全部通过。
- 多句来源证据只发送不重叠句段；测试反向证明拼接后与冻结原文一致。
- 输出校验重试只携带校验原因和原始请求，不再回灌整份无效响应。
- 每次模型调用在进入 Runtime 前持久化字符数、字节数、估算 Token、分区长度、排队时间和节点预算；超过硬预算直接失败。
- 审计问题成立性确认使用单任务节点并发额度有界并行；确认排除的“原文来源未落实”写入统一来源处置，后续修正不再要求重复补齐。
- 最终澄清一致性检查只读取开放澄清实际关联的需求和来源，不再发送全部 228 个来源单元和全部需求。
- pipeline 11 从任务创建时保存唯一 20 分钟截止时间；单次调用上限为 120 秒且不越过任务剩余时间，恢复和重试不重置截止时间。
- 本地 `npm start` 已重启；Vite 返回 HTTP 200 且包含根挂载点，Electron 主进程 PID 220228，三个 Electron 子进程均由该主进程启动。

## 尚未收敛

- 本轮证明了程序预算、来源处置和调度约束，没有用真实 Runtime 完成三次冻结 PRD 新任务及五任务并发。
- 修正生成与独立复核仍在各问题组内部串行；本轮先消除了 45 次成立性确认的全局串行链，真实任务数据将决定是否还需把互不冲突的修正组改成两阶段并行提交。
- 因真实任务验收尚未完成，SG10、SG11 均保持进行中，不能宣称 20 分钟目标已经达标。
