# Round 9：pipeline 12 提示预算与并行收敛验证

## 已通过

- `npm test -- --run`：16 个测试文件、183 项测试全部通过。
- `npm run typecheck`：应用与 Electron TypeScript 检查通过。
- `npm run build`：TypeScript、Vite 生产构建及 Electron TypeScript 编译通过。
- 大功能在进入模型前按来源包拆分；自动化证明实际请求均未越过对应节点硬预算。
- 证据别名缩短为单次请求内的 `E1`、`E2`，提示中删除解析元数据；句段拼接仍与冻结原文逐字符一致。
- 每次实际模型尝试均重新统计完整提示并记录尝试序号、请求哈希与预算；校验重试不携带上一轮无效响应。
- 每次重试按任务剩余总时间重新计算调用超时，不能通过重试获得新的 120 秒窗口。
- 同一功能的成立性确认批量执行；无冲突修正和局部复核有界并行，结果提交、问题状态和检查点通过单写入队列串行落盘。
- 来源已承接决定必须列出真实需求 ID，并绑定需求与来源依赖哈希；承接内容变化后决定自动失效。
- `review-ready` 恢复直接提交已保存复核结果，不重复生成候选或调用复核。
- 最终检查绑定完成验证时的结果版本及内容指纹；证明失效时保持 `unknown`，不能由开放问题数反推通过。
- 本地 `npm start` 已重启；Vite 返回 HTTP 200 且存在根挂载点，Electron 主进程 PID 743564，三个子进程均由该主进程启动。

## 尚未收敛

- 本轮完成程序级根治和真实桌面加载验证，没有自动发起用户选择文件后的真实 PRD 任务。
- 冻结 PRD 连续三次新任务及五任务并发的 20 分钟业务验收尚未执行，因此 SG10、SG11 继续保持进行中。
- 20 分钟目标当前由总截止和请求预算保证失败可控；是否能在真实 Runtime 下稳定形成可交付结果，必须由上述真实验收确认。
