# Round 6：执行过程反证与验收结论纠正

本轮仅只读检查源码和持久化任务，不改业务代码、不编辑任务 JSON、不重新执行模型。代码基线 `1d77b75`。

## 确认的反证

- 任务文件：`C:/Users/64554/AppData/Roaming/prd-refinement-desktop/analysis-tasks/T-9695DD2E.json`。
- attempt=9，runtimeMetrics=221；21:38:30 至 22:50:50，约 72.3 分钟，含调试中断；25 条输出校验失败诊断。
- 三条模型调用分别为 `prd-T-9695DD2E-a5-repair-A-0011-round1-78-try1`、`prd-T-9695DD2E-a7-repair-A-0011-round1-92-try1`、`prd-T-9695DD2E-a8-repair-A-0011-round1-104-try1`。目标均是点击数据集名称跳转详情。
- repairs 第 3 条补齐跳转行为；第 8、13 条 before 与 candidate 全对象 JSON 完全相等，仍为 accepted。A-0009/A-0010 在第 7、12 条也发生相同的无变化提交。
- Q-0006/Q-0007 都询问确认合并是否直接生成版本，最终仍同时为 blocking。open=0 不能证明无平台整理错误。
- PRD 正文、流程和 AC-09 明确确认合并后进入独立弹窗、不直接生成版本；原型短标签为“确认合并生成版本”。必须先评估语义层次和来源约束，不能直接把重复冲突发给用户。

## 原因链与排除

`scheduler-v2.ts:690` 把在局部修复中调用的 audit 确认失败标为审计阶段失败；恢复时 `:535` 从缓存重新生成并编号问题数组，覆盖已修复状态。确认集合只存 ID（`:599`），不会因依赖内容变化自动失效。新审计也会复用旧 A 编号，历史修复记录不能可靠证明当前问题的预算或结论。

重复修复并非仅 UI 重复显示：以上三个独立 runtime session 和两次完全相等的 before/candidate 已证明真实模型调用。也不只是新发现问题恰好共用编号：第 3、8、13 条历史 originalIssues 都指出同一条名称点击行为，后两次 before 已具备该行为。

最终澄清重复不是“同一来源承载不同问题”的正常现象：两项的唯一未决点都是确认按钮是否直接生成版本。是否确实构成业务阻塞仍需完整语义审查，本轮不替业务方裁决原型优先级。

## 状态与后续

撤回 round 5 和 SG9 的根治通过结论；保留单元测试、构建和页面读回的历史事实。新方案为 `docs/spec/task-execution-state-contract.md`，要求问题身份、结果版本、工作项恢复、原子提交、最终检查形成统一契约。

本轮没有新的通过运行。下一次验收必须在固定代码、输入和模型配置下创建全新任务，中途改代码或手动救场则该次验收不通过。旧任务只作为失败复现样本。
