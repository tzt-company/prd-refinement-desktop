# PRD 选择入口回归

日期：2026-09-11。

用户指出资料包工作台替换了原来效果较好的 PRD 选择页。本轮将新建任务入口恢复为居中单卡片，并保留已经实现的资料包能力：

- 初始页面只展示“选择 PRD 文件”主操作，支持 HTML、DOC、DOCX、PDF、Markdown、TXT 和文件拖入。
- 首次选择时由界面在后台建立资料包，不再要求用户先执行“新建资料包”。
- 主 PRD 保存后，才展示更换主文档、补充文件、资料目录、用途、挂载名称和索引操作。
- 已有资料保留“继续上次准备”入口；索引、缺件、来源分页和分析门禁行为不变。

验证结果：

- `npm test`：12 个测试文件、130 项测试通过。
- `npm run build`：TypeScript、Electron 和 Vite 生产构建通过。
- `npm run verify:premium`：UI 契约检查通过。
- 渲染层流程：初始补充资料区域数量为 0；选择主 PRD 后为 1；目录、索引、检索、来源预览和分析交接通过；1440/1100 无横向溢出。
- 隔离 Electron：真实 File 拖放、主 HTML、补充目录和索引流程通过。
- 当前桌面已重启并加载生产脚本 `index-CdZYlgUu.js`；实际入口的补充资料区域为 0，截图及读回见 `round-3/desktop-entry.png` 和 `desktop-entry.json`。

本轮只调整新建任务的信息层级和视觉入口，没有修改资料快照、解析、索引或分析逻辑。
