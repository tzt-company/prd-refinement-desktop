# 阻塞事项建议方案验收报告

每个新生成的阻塞业务事项已包含可直接采纳的建议、依据、影响、确认点及至多两个备选。旧任务可在待处理事项页单独生成缺少的建议，无需重跑完整细化。

用户可在列表连续勾选多项建议，并通过唯一的任务级调整入口一次提交；补充文字优先于所选建议。未采纳建议不会修改需求或关闭事项，交付包与 Excel 均明确标记建议尚未确认。

单条建议支持按需原地修改。修改稿按结果版本保存在本机，保存后自动加入本次调整，未修改事项仍保持批量操作。

验证结果以 `matrix.csv` 和 `round-1.md` 为准。
